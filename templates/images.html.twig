{% if value is not null %}
    <div class="form-widget">
        <div class="form-control">
            {% for image in value %}
                <div  class="easyadmin-thumbnail" data-featherlight="#easyadmin-lightbox-{{ item.id }}" data-featherlight-close-on-click="anywhere">
                    <img src="/wdev_maya/eindwerk/images/products/{{ image.image }}">
                </div>

                <div id="easyadmin-lightbox-{{ item.id }}" class="easyadmin-lightbox">
                    <img src="/wdev_maya/eindwerk/images/products/{{ image.image }}">
                </div>
            {% endfor %}
        </div>
    </div>
{% endif %}